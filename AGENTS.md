## 项目概述

本项目是一个 Coze 工作流配置，用于「每日读书视频」的自动化创作。

**工作流功能：** 输入一本书名，自动生成 8 本名著书单（含豆瓣封面搜索），并为每本书提取金句、合成语音、生成字幕数据，最终输出剪映草稿。

## 技术栈

- **平台：** Coze.cn（扣子）
- **工作流编排：** Coze Workflow（JSON 配置）
- **核心模型：**
  - 豆包·1.5·Pro·32k（书名生成）
  - DeepSeek-V3（金句生成）
- **插件集成：**
  - 豆瓣搜书（书籍封面搜索）
  - 语音合成（TTS）
  - 剪映小助手（视频草稿生成）

## 目录结构

```
/workspace/projects/
├── .coze                    # 项目配置
├── AGENTS.md                # 本文件
├── README.md                # 项目说明
├── book.json                # Coze 工作流完整配置（核心文件）
└── 书单高级代码 (1).txt      # 工作流 JSON 副本
```

## 关键入口

- **工作流配置文件：** `book.json`
- **工作流 ID：** `7508664931718938676`
- **空间 ID：** `7508203337217818643`

## 工作流节点说明

1. **随机生成8本书的书名**（LLM - 豆包）
   - 输入：用户指定的书籍名称
   - 输出：8 本名著书名列表（用户输入的书固定在第 3 本）

2. **搜索每一本书的封面**（豆瓣搜书 - 批处理）
   - 输入：书名列表
   - 输出：书籍封面、作者、豆瓣链接

3. **作者名提取**（文本处理）
   - 从搜索结果中提取第 3 本书的作者名

4. **书本金句生成**（LLM - DeepSeek-V3）
   - 输入：书名 + 作者
   - 输出：约 200 字经典语句，每句话不超过 15 字

5. **批处理-音频生成**（循环）
   - 将每句文案转为语音（高冷御姐音色）

6. **获取音频时间线**
   - 获取各音频片段的起止时间

7. **字幕格式整理**
   - 根据时间线生成字幕数据

8. **创建剪映草稿**
   - 分辨率：1920x1080

9. **添加书籍封面素材**

10. **添加音频素材**

11. **添加字幕素材**

12. **保存草稿**
    - 输出：剪映草稿 URL

## 运行与预览

本项目为 Coze 工作流配置，无需本地运行。可通过以下方式使用：

1. 将 `book.json` 内容导入 Coze 平台
2. 在 Coze 工作流编辑器中打开
3. 填写工作流 ID `7508664931718938676` 进行编辑

## 用户偏好与长期约束

- 工作流配置已完整保存于 `book.json`
- 修改工作流后建议同步更新 `AGENTS.md` 中的节点说明
- 本地仅作为工作流配置文件的版本管理使用

## 常见问题和预防

- **Q：工作流导入 Coze 失败？**
  - A：检查 `book.json` 是否为有效 JSON 格式
- **Q：插件节点报错？**
  - A：确认 Coze 账号已开通对应插件（豆瓣搜书、语音合成、剪映小助手）
